<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="settings"
            type="de.spitak.amazinggame.viewmodel.SettingsViewModel"
            />
    </data>

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_settings"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="de.spitak.amazinggame.activity.SettingsActivity"
        >

        <EditText
            android:id="@+id/settings_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:addTextChangedListener="@{settings.watcher}"
            android:hint="@string/settings_name"
            android:text="@{settings.settings.name}"
            />
    </RelativeLayout>
</layout>